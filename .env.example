# ============================================
# Variables de entorno para InfoCurso
# ============================================
# IMPORTANTE: Copia este archivo a .env y configura los valores
# En Coolify: configura estas variables en la UI del proyecto

# --------------------------------------------
# Database
# --------------------------------------------
DB_PASSWORD=tu_password_seguro_aqui

# --------------------------------------------
# Backend (JWT)
# --------------------------------------------
# Genera un secret seguro de al menos 256 bits
# Puedes usar: openssl rand -base64 32
JWT_SECRET=tu_jwt_secret_muy_largo_y_seguro_minimo_256_bits

# --------------------------------------------
# URLs (IMPORTANTE para Coolify)
# --------------------------------------------
# Usa las URLs que Coolify te asigna para cada servicio
# Ejemplo con dominios de Coolify:
#   FRONTEND_URL=https://infocurso-frontend.tu-servidor.com
#   NEXT_PUBLIC_API_URL=https://infocurso-backend.tu-servidor.com/api

# URL del frontend (para CORS del backend)
FRONTEND_URL=https://tu-frontend-url.com

# URL del backend API (para el frontend)
NEXT_PUBLIC_API_URL=https://tu-backend-url.com/api
